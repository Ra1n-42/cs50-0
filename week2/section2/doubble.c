#include <cs50.h>
#include <stdio.h>


int main(void)
{
    int sequence[5];
    sequence[0] = 1;

    printf("%i\n", sequence[0]);

    for (int i = 01; i < 5; i++)
    {
        sequence[i] -= 1
    }
}
