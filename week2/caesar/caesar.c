#include <cs50.h>
#include <stdio.h>
#include <string.h>



int main(int argc, string argv[])
{
    int k = toint(argv[1]);
    string text = "abc";
    printf("%i", k);

    // int len = strlen(text);
    // for (int i = 0; i < len; i++)
    // {
    //     printf("%c", text[i] + k);
    // }
    printf("\n");
}
